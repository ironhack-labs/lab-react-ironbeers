import { Link } from 'react-router-dom';

function Navbar() {
        return(
            <Link to="/">
                <header>
                </header>
            </Link>
        )
    
}

export default Navbar;