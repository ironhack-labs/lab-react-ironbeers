import React from 'react';
import { NavLink } from 'react-router-dom';


const Navbar = () => {
    return (
        <div id="navbar">
        <NavLink to={'/'}>🍺</NavLink>
        </div>
    );
}

export default Navbar;
