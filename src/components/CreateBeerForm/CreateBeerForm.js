import { useState } from "react"
import { Form, Button, Row, Col } from "react-bootstrap"
import beersService from "../../services/beers.services"

const CreateBeerForm = ({ closeModal, updateList }) => {
    return (
        <h1>a creaaar</h1>
    )
}

export default CreateBeerForm