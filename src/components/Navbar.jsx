import { Link } from "react-router-dom";

    function Navbar() {
//Tentar criar um import para a imagem tp : import imagr(ouwtv) from: a pasta de onde a imagem esta
    return(
        <nav>
            <Link to="/">Home</Link>
        </nav>
    )
}

export default Navbar;
