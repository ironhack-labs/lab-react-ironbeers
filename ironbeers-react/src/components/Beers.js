import React, { Component } from 'react';
import logo from './logo.svg';
import './App.css';

const beer = (props) => {
    return(
        <div>
            <button>Home</button>
        </div>
    )
}

export default beer;