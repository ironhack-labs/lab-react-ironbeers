import React from 'react';
import { Options } from '../components/Options';
import { Container } from '../styles/HomePage';

export const Home = () => {
  return (
    <Container>
      <Options />
    </Container>
  );
};
