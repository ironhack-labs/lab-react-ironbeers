import React from 'react';
import "./Form.scss";

const Form = (props) => {
    debugger
    return (
        <div className="form">
            {props.children}
        </div>
    );
}

export default Form;